---
apiVersion: v1
kind: Service
metadata:
  name: mypyspark
  labels:
    name: mypyspark
spec:
  type: NodePort
  ports:
  - port: 5000
    name: mypysparkapp-port
    targetPort: 5000
    protocol: TCP
    nodePort: 30005
  selector:
    name: mypysprk